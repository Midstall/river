name: CI

on:
  pull_request:
  workflow_dispatch:
  merge_group:
  push:
    branches:
      - master

jobs:
  DeterminateCI:
    uses: DeterminateSystems/ci/.github/workflows/workflow.yml@main
    permissions:
      id-token: write
      contents: read
    with:
      visibility: public
